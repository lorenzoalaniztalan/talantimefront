name: schedule_repository
description: A repository that handles schedule related requests
version: 0.1.0+1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http_schedule_api:
    path: ../http_schedule_api
  schedule_api:
    path: ../schedule_api

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  very_good_analysis: ^5.0.0
